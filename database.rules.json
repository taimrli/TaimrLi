//{"rules":{"users":{"$uid":{".write":"auth !== null && auth.uid === $uid",".read":"auth !== null && auth.uid === $uid"}}}}
{
	"rules": {
		"users": {
			"$uid": {
				".write": "true", //"auth !== null && auth.uid === $uid",
				".read": "true" //auth !== null && auth.uid === $uid"
			}
		}
	}
}